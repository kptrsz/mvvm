<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_margin="@dimen/mini_margin"
    android:elevation="2dp"
    android:background="@color/colorItemBackground"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="wrap_content">


    <TextView
        android:id="@+id/tv_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:layout_marginTop="16dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/colorAccent"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/iv_recycler"
        app:layout_constraintTop_toTopOf="@+id/iv_recycler"
        tools:text="Title" />

    <ImageView
        android:id="@+id/iv_recycler"
        android:layout_width="@dimen/image_width"
        android:layout_height="@dimen/image_width"
        android:scaleType="centerCrop"
        android:src="@drawable/img_snow"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:ellipsize="end"
        android:layout_marginBottom="16dp"
        android:text="@string/userid"
        app:layout_constraintBottom_toBottomOf="@+id/iv_recycler"
        app:layout_constraintStart_toEndOf="@+id/iv_recycler" />

    <TextView
        android:id="@+id/tv_user_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:ellipsize="end"
        tools:text="9"
        app:layout_constraintBottom_toBottomOf="@+id/textView"
        app:layout_constraintStart_toEndOf="@+id/textView"
        app:layout_constraintTop_toTopOf="@+id/textView" />

    <TextView
        android:id="@+id/tv_body"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:ellipsize="end"
        android:maxLines="2"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/iv_recycler"
        app:layout_constraintTop_toBottomOf="@+id/tv_title"
        tools:text="Body" />

    <TextView
        android:id="@+id/tv_id_marker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:text="@string/id"
        android:ellipsize="end"
        app:layout_constraintBottom_toBottomOf="@+id/tv_user_id"
        app:layout_constraintStart_toEndOf="@+id/tv_user_id"
        app:layout_constraintTop_toTopOf="@+id/tv_user_id" />

    <TextView
        android:id="@+id/tv_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:ellipsize="end"
        tools:text="2"
        app:layout_constraintBottom_toBottomOf="@+id/tv_id_marker"
        app:layout_constraintStart_toEndOf="@+id/tv_id_marker"
        app:layout_constraintTop_toTopOf="@+id/tv_id_marker" />
</androidx.constraintlayout.widget.ConstraintLayout>